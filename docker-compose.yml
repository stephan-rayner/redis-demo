version: "3"

services:

  redis:
    image: redis
    ports:
      - "6379:6379"  
    # volumes:
    #   - ./redis_data/dump.rdb:/data redis:latest

  app:
    build: ./src
    volumes:
      - ./src:/application/src
    depends_on:
      - redis